<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>BLOG</title>
    <link rel="stylesheet" href="/css/style.css" />
    <script src="/send_url.js"></script>
  </head>
  <body>
    <%- include('header'); %>
    <main>
      <div class="article">
        <% if (article.category === 'all') { %>
        <h1><%= article.title %></h1>
        <% if (article.image_path) { %>
        <img
          src="<%= article.image_path %>"
          alt="記事画像"
          style="max-width: 100%; height: auto"
        />
        <% } %>
        <p><%= article.content %></p>
        <a href="/article/edit/<%= article.id %>" class="btn">編集する</a>
        <% } %> <% if (article.category === 'limited') { %>
        <i>会員限定</i>
        <h1><%= article.title %></h1>
        <% if (locals.isLoggedIn) { %> <% if (article.image_path) { %>
        <img
          src="<%= article.image_path %>"
          alt="記事画像"
          style="max-width: 100%; height: auto"
        />
        <% } %>
        <p><%= article.content %></p>
        <a href="/article/edit/<%= article.id %>" class="btn">編集する</a>
        <% } else { %>
        <div class="article-login">
          <p>今すぐログインしよう！</p>
          <p>記事の続きは<br />ログインすると読むことができます</p>
          <img src="/images/login.svg" />
          <a class="btn" href="/login">ログイン</a>
        </div>
        <% } %> <% } %>
      </div>
    </main>
    <footer>
      <a class="btn sub" href="/list">一覧にもどる</a>
    </footer>
  </body>
</html>
