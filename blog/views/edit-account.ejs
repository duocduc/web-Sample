<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>アカウント情報編集</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <%- include('header'); %>
    <div class="sign">
      <form
        action="/edit-account/<%= user.id %>"
        method="POST"
        enctype="multipart/form-data"
      >
        <p><strong>ユーザー名:</strong></p>
        <input
          type="text"
          name="username"
          value="<%= user.username %>"
          required
        />

        <p><strong>メールアドレス:</strong></p>
        <input type="email" name="email" value="<%= user.email %>" required />

        <p><strong>プロフィール画像:</strong></p>
        <% if (user.profile_image) { %>
        <img
          src="<%= user.profile_image %>"
          alt="プロフィール画像"
          class="profile-image"
        />
        <% } %>
        <input type="file" name="profileImage" />
        <input
          type="hidden"
          name="existingImage"
          value="<%= user.profile_image %>"
        />

        <input type="submit" value="更新する" class="back-link" />
        <a href="/account/<%= user.id %>">戻る</a>
      </form>
    </div>
  </body>
</html>
