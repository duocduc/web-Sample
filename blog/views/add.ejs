<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>記事を追加</title>
    <link rel="stylesheet" href="/css/style.css" />
    <script src="/send_url.js"></script>
  </head>
  <body>
    <div class="sign">
      <div class="container">
        <h1><a href="/list">ブログ</a></h1>
        <div class="panel">
          <h2>記事を追加</h2>
          <form
            action="/add-article"
            method="POST"
            enctype="multipart/form-data"
          >
            <p>タイトル:</p>
            <input type="text" name="title" required /><br />
            <p>概要:</p>
            <input type="text" name="summary" required /><br />
            <p>記事の内容:</p>
            <textarea name="content" required></textarea><br />
            <p>カテゴリー:</p>
            <select name="category" required>
              <option value="all">全体公開 (All)</option>
              <option value="limited">会員限定 (Limited)</option></select
            ><br />
            <p>画像を選択:</p>
            <input type="file" name="image" accept="image/*" /><br />
            <input type="submit" value="記事を投稿する" />
            <a href="/list">一覧に戻る</a>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
